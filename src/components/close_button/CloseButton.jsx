import React from 'react';
import {TouchableOpacity} from "react-native";

const CloseButton = () => {
    return (
        <TouchableOpacity>

        </TouchableOpacity>
    );
};

export default CloseButton;