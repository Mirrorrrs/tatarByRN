import React from 'react';
import ArSceneScreen from "../features/ar_scene_screen/ArSceneScreen";

const ArScene = ({navigation, route}) => {
    return <ArSceneScreen navigation={navigation} route={route}/>;
};

export default ArScene;